package com.krementransport.ui.theme

import androidx.compose.material3.Typography

/** Material 3 defaults. The app has no custom face; route badges set their own weight locally. */
val AppTypography = Typography()
